# Stage 2: Team Task Breakdown (Strictly P0)

Завдання Stage 2: Написати повноцінні тести (Arrange-Act-Assert) для кожної P0 історії. Але **саму програму писати не треба** — замість неї ми створюємо "заглушки" (stubs), які викликають `raise NotImplementedError`, щоб тести падали красиво і зрозуміло (червоні тести).

Кожен учасник для своєї історії:

1. Пише ручний тест у `test_plan.md` (Part C).
2. Пише мінімум 2 автотести в `tests/` (1 happy path + 1 edge case).
3. Вписує свої тести в Inventory (§2) та Traceability Matrix (§3).

---

## 🚀 Вадим К (Інфраструктура + US-01) — ЗРОБЛЕНО ✅

Ти підготував майданчик і закрив першу (і найскладнішу) історію.

- `[x]` **Інфраструктура:** Налаштував `pytest`, створив CI (`test.yml`), заповнив метадані (§0) і стратегію (§1) в `test_plan.md`.
- `[x]` **Історія US-01:** Створив `test_moodle_fetcher.py` та `test_classroom_fetcher.py`, додав ручний тест, мапинг у матрицю.
- `[x]` **AI Usage:** Заповнив свою сесію з розповіддю про відхилені тести.

---

## 👤 Тіммейт 1 (US-02 та US-03)

_Бере на себе логіку форматування, сортування та обробки дат._

- `[ ]` **Історія US-02 (Sorting) & US-03 (Midnight cutoff):**
  - Ознайомитися з сигнатурою `format_assignment` у `docs/design_doc.md`.
  - Створити файл `tests/test_formatter.py`.
  - Написати автотести в `test_formatter.py` (тестуємо правильне сортування; алфавітний тайбрейкер; перевірку, що 00:00 та 23:59 рендеряться коректно).
  - Додати 2 ручних тести в `test_plan.md` (§7 Manual test cases).
- `[ ]` **Документація `test_plan.md`:**
  - Внести свої тести в Inventory (§2) замість `TBD (teammate)`.
  - Змапити їх на US-02 та US-03 в Traceability Matrix (§3).
  - Описати граничні випадки в §4 (Edge cases deliberately included).
  - Описати те, що не тестується, в §5 (What I am explicitly not testing).
- `[ ]` **AI Usage:** Зробити свій запис (Session 3) про відхилений тест в `ai_usage.txt`.

---

## 👤 Тіммейт 2 (US-04 + Адміністрування)

_Бере на себе перевірку помилок авторизації та закриття етапу Stage 2._

- `[ ]` **Історія US-04 (Auth Error):**
  - Дописати автотести в уже існуючі `tests/test_moodle_fetcher.py` та `tests/test_classroom_fetcher.py` на випадок `invalid token` (мають падати або піднімати очікувану помилку).
  - Додати свій ручний тест в `test_plan.md` (§7).
  - Додати свої тести в Inventory (§2) та Traceability Matrix (§3) замість `TBD`.
- `[ ]` **Адміністрування (CONTRIBUTIONS & Підписи):**
  - Оновити `docs/CONTRIBUTIONS.md`. Додати розділ "Stage 2 Contributions" і коротко написати, хто які історії та тести покрив.
  - Зібрати підписи від усіх у `test_plan.md` (§8 Sign-off).
  - Перевірити вкладку Actions у GitHub — CI має бути "червоним", але не через синтаксичну помилку, а тому що тести впали з `AssertionError`.
- `[ ]` **AI Usage:** Зробити свій запис (Session 4) про відхилений тест в `ai_usage.txt`.
